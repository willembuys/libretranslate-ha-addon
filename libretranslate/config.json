{
  "name": "LibreTranslate",
  "version": "latest",
  "slug": "libretranslate",
  "description": "Selbstgehosteter LibreTranslate-Server für Home Assistant, komplett offline.",
  "startup": "services",
  "boot": "auto",
  "ports": {
    "5000/tcp": 5000
  },
  "map": ["share"],
  "options": {
    "host": "0.0.0.0",
    "port": 5000,
    "languages": "en,es,de,fr,it,zh",
    "debug": false,
    "webui": true
  },
  "schema": [
    {"name": "host", "required": true, "type": "string"},
    {"name": "port", "required": true, "type": "integer"},
    {"name": "languages", "required": true, "type": "string"},
    {"name": "debug", "required": true, "type": "bool"},
    {"name": "webui", "required": true, "type": "bool"}
  ],
  "arch": ["amd64"],
  "image": "libretranslate/libretranslate"
}
